# Los saltos de línea se guardan siempre al estilo Unix. Railway compila en
# Linux y un script con saltos de Windows falla con «bad interpreter».
* text=auto eol=lf

# Binarios: que Git no los toque.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.mp4 binary
